<Window x:Class="DeepDroidChanger.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding Caption}"
        Width="520"
        MinWidth="440"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.WindowBackground}"
        TextElement.Foreground="{DynamicResource Brush.TextPrimary}"
        FontFamily="{StaticResource Metric.FontFamily}">
    <Grid Margin="{StaticResource Spacing.PageMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource CardBorderStyle}"
                Padding="{StaticResource Spacing.CardPadding}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon x:Name="WarningIcon"
                                         Kind="Alert"
                                         Width="32"
                                         Height="32"
                                         VerticalAlignment="Top"
                                         Foreground="{DynamicResource Brush.Warning}"/>
                <TextBlock x:Name="ConfirmationMessage"
                           Grid.Column="2"
                           Text="{Binding Message}"
                           Style="{StaticResource AppLabelTextStyle}"
                           TextWrapping="Wrap"
                           MaxWidth="420"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="NoButton"
                    Grid.Column="1"
                    MinWidth="88"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"
                    Content="{DynamicResource ConfirmationDialog_NoButton}"
                    Style="{StaticResource FlatButtonStyle}"
                    IsCancel="True"
                    IsDefault="True"/>
            <Button x:Name="YesButton"
                    Grid.Column="2"
                    MinWidth="88"
                    Command="{Binding ConfirmCommand}"
                    Style="{StaticResource PrimaryButtonStyle}">
                <TextBlock x:Name="YesButtonText"
                           Text="{DynamicResource ConfirmationDialog_YesButton}"
                           Foreground="{DynamicResource Brush.AccentForeground}"/>
            </Button>
        </Grid>
    </Grid>
</Window>
